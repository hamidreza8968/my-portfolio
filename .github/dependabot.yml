version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 10
    # Optionally ignore packages here
    # ignore:
    #   - dependency-name: "left-pad"

